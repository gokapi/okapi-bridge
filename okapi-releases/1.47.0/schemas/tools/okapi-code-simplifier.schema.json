{
  "$id": "okapi:code-simplifier",
  "title": "Inline Codes Simplifier",
  "type": "object",
  "x-component": {
    "id": "okapi:code-simplifier",
    "type": "step",
    "displayName": "Inline Codes Simplifier",
    "description": "Merges adjacent inline codes in the source part of a text unit. Also where possible, moves leading and trailing codes of the source to the skeleton (Only for GenericSkeleton!). Expects: filter events. Sends back: filter events."
  },
  "properties": {
    "removeLeadingTrailingCodes": {
      "type": "boolean",
      "default": true,
      "description": "Removes leading and trailing codes from the source and place them in the skeleton.",
      "title": "Remove leading and trailing codes"
    },
    "mergeCodes": {
      "type": "boolean",
      "default": true,
      "description": "Merges adjacent codes (leading/trailing codes are removed first (if option is enabled)).",
      "title": "Merge codes"
    }
  }
}