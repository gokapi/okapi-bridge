{
  "$id": "xliff-joiner",
  "title": "XLIFF Joiner",
  "type": "object",
  "description": "Join multiple XLIFF documents into one. Expects: raw document. Sends back: raw document.",
  "x-step": {
    "class": "net.sf.okapi.steps.xliffsplitter.XliffJoinerStep",
    "parameterMappings": [
      "OUTPUT_URI",
      "SOURCE_LOCALE",
      "TARGET_LOCALE"
    ],
    "eventHandlers": [
      "handleEndBatch",
      "handleRawDocument",
      "handleStartBatch"
    ],
    "interfaces": [],
    "inputType": "filter-events",
    "outputType": "file"
  },
  "properties": {
    "inputFileMarker": {
      "type": "string",
      "default": "_PART",
      "title": "Input file marker",
      "x-editor": {
        "widget": "text"
      },
      "description": "Marker text in input filenames used to identify and group split XLIFF files for joining."
    },
    "outputFileMarker": {
      "type": "string",
      "default": "_CONCAT",
      "title": "Output file marker",
      "x-editor": {
        "widget": "text",
        "text": {
          "allowEmpty": true
        }
      },
      "description": "Marker text used to generate the output filename(s) by replacing the input marker and index."
    }
  }
}
