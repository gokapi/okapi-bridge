{
  "$id": "segmentation",
  "title": "Segmentation",
  "type": "object",
  "description": "Apply SRX segmentation to the text units content of a document. Expects: filter events. Sends back: filter events.",
  "x-step": {
    "class": "net.sf.okapi.steps.segmentation.SegmentationStep",
    "parameterMappings": [
      "INPUT_ROOT_DIRECTORY",
      "ROOT_DIRECTORY",
      "SOURCE_LOCALE",
      "TARGET_LOCALE",
      "TARGET_LOCALES"
    ],
    "eventHandlers": [
      "handleCustom",
      "handleStartBatch",
      "handleStartBatchItem",
      "handleStartDocument",
      "handleTextUnit"
    ],
    "interfaces": [],
    "inputType": "filter-events",
    "outputType": "filter-events"
  },
  "properties": {
    "forceSegmentedOutput": {
      "type": "boolean",
      "default": true,
      "title": "Force Segmented Output",
      "description": "Force output formats to show segmentation regardless of filter configuration."
    },
    "overwriteSegmentation": {
      "type": "boolean",
      "default": false,
      "title": "Overwrite Existing Segmentation",
      "description": "Re-segment already-segmented text units, replacing previous segmentation."
    },
    "deepenSegmentation": {
      "type": "boolean",
      "default": false,
      "title": "Deepen Existing Segmentation",
      "description": "Keep existing segmentation but split segments further using the current SRX rules."
    },
    "sourceSrxPath": {
      "type": "string",
      "default": "",
      "title": "Source SRX Rules Path",
      "description": "Path to the SRX segmentation rules file for segmenting source text."
    },
    "targetSrxPath": {
      "type": "string",
      "default": "",
      "title": "Target SRX Rules Path",
      "description": "Path to the SRX segmentation rules file for segmenting target text."
    },
    "sourceSrx": {
      "type": "string",
      "default": "",
      "title": "Source SRX Content",
      "description": "Inline SRX segmentation rules content for the source language, as an alternative to a file path."
    },
    "targetSrx": {
      "type": "string",
      "default": "",
      "title": "Target SRX Content",
      "description": "Inline SRX segmentation rules content for the target language, as an alternative to a file path."
    },
    "renumberCodes": {
      "type": "boolean",
      "default": false,
      "title": "Renumber Code IDs",
      "description": "Renumber inline code IDs in each segment to start at 1."
    },
    "segmentSource": {
      "type": "boolean",
      "default": true,
      "title": "Segment Source Text",
      "description": "Segment the source text of text units using SRX rules."
    },
    "segmentTarget": {
      "type": "boolean",
      "default": false,
      "title": "Segment Target Text",
      "description": "Segment existing target text using SRX rules for the processed locales."
    },
    "checkSegments": {
      "type": "boolean",
      "default": true,
      "title": "Verify Segment Alignment",
      "description": "Verify that target segments match source segments when target content exists."
    },
    "trimSrcLeadingWS": {
      "type": "integer",
      "default": -1,
      "title": "Trim Source Leading Whitespace",
      "description": "Maximum leading whitespace characters to trim from source segments (-1 for default)."
    },
    "trimSrcTrailingWS": {
      "type": "integer",
      "default": -1,
      "title": "Trim Source Trailing Whitespace",
      "description": "Maximum trailing whitespace characters to trim from source segments (-1 for default)."
    },
    "trimTrgLeadingWS": {
      "type": "integer",
      "default": -1,
      "title": "Trim Target Leading Whitespace",
      "description": "Maximum leading whitespace characters to trim from target segments (-1 for default)."
    },
    "trimTrgTrailingWS": {
      "type": "integer",
      "default": -1,
      "title": "Trim Target Trailing Whitespace",
      "description": "Maximum trailing whitespace characters to trim from target segments (-1 for default)."
    },
    "treatIsolatedCodesAsWhitespace": {
      "type": "boolean",
      "default": false,
      "title": "Treat Isolated Codes as Whitespace",
      "description": "Treat isolated inline codes as whitespace during segmentation."
    },
    "doNotSegmentIfHasTarget": {
      "type": "boolean",
      "default": false,
      "title": "Skip Segmentation if Target Exists",
      "description": "Do not segment text units that already have target content."
    }
  }
}
