{
  "$id": "search-and-replace",
  "title": "Search and Replace",
  "type": "object",
  "description": "Performs search and replace on the entire file or the text units. Expects raw document or filter events. Sends back: raw document or filter events.",
  "x-step": {
    "class": "net.sf.okapi.steps.searchandreplace.SearchAndReplaceStep",
    "parameterMappings": [
      "INPUT_ROOT_DIRECTORY",
      "OUTPUT_URI",
      "ROOT_DIRECTORY",
      "SOURCE_LOCALE",
      "TARGET_LOCALE"
    ],
    "eventHandlers": [
      "handleEndBatch",
      "handleRawDocument",
      "handleStartBatch",
      "handleTextUnit"
    ],
    "interfaces": [],
    "inputType": "filter-events",
    "outputType": "file"
  },
  "properties": {
    "regEx": {
      "type": "boolean",
      "default": false,
      "title": "Use Regular Expressions",
      "description": "Enable Java regular expression mode for all search patterns."
    },
    "dotAll": {
      "type": "boolean",
      "default": false,
      "title": "Dot Also Matches Line-Feed",
      "description": "Make the period character match every character including line-feed."
    },
    "ignoreCase": {
      "type": "boolean",
      "default": false,
      "title": "Ignore Case Differences",
      "description": "Ignore case when matching search patterns."
    },
    "multiLine": {
      "type": "boolean",
      "default": false,
      "title": "Multiline Mode",
      "description": "Make ^ and $ match at the beginning and end of each line."
    },
    "target": {
      "type": "boolean",
      "default": true,
      "title": "Replace in Target Content",
      "description": "Perform search and replace on the target content of text units."
    },
    "source": {
      "type": "boolean",
      "default": false,
      "title": "Replace in Source Content",
      "description": "Perform search and replace on the source content of text units."
    },
    "replaceALL": {
      "type": "boolean",
      "default": true,
      "title": "Replace All Instances",
      "description": "Replace all matches found instead of only the first match."
    },
    "count": {
      "type": "boolean",
      "default": false,
      "title": "Replacement Count",
      "description": "Total number of replacements performed."
    },
    "replacementsPath": {
      "type": "string",
      "default": "",
      "title": "Replacements File Path",
      "description": "Path to a tab-delimited UTF-8 file with search and replacement pairs."
    },
    "logPath": {
      "type": "string",
      "default": "${rootDir}/replacementsLog.txt",
      "title": "Replacement Log File Path",
      "description": "Path where the log of performed replacements will be saved."
    },
    "saveLog": {
      "type": "boolean",
      "default": false,
      "title": "Save Replacement Log",
      "description": "Save a log of all replacements performed to the specified file."
    }
  }
}
