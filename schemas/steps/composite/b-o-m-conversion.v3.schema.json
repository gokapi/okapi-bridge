{
  "$id": "b-o-m-conversion",
  "title": "BOM Conversion",
  "type": "object",
  "description": "Add or remove Unicode Byte-Order-Mark (BOM) in a text-based file. Expects: raw document. Sends back: raw document.",
  "x-step": {
    "class": "net.sf.okapi.steps.bomconversion.BOMConversionStep",
    "parameterMappings": [
      "OUTPUT_URI",
      "SOURCE_LOCALE",
      "TARGET_LOCALE"
    ],
    "eventHandlers": [
      "handleEndBatchItem",
      "handleRawDocument",
      "handleStartBatchItem"
    ],
    "interfaces": [],
    "inputType": "filter-events",
    "outputType": "file"
  },
  "properties": {
    "removeBOM": {
      "type": "boolean",
      "default": false,
      "title": "Remove BOM If Present",
      "description": "Remove the Byte-Order-Mark from input files if detected; when disabled, adds a BOM if not already present."
    },
    "alsoNonUTF8": {
      "type": "boolean",
      "default": false,
      "title": "Remove UTF-16 BOMs Also",
      "description": "Also remove the BOM from UTF-16 files, not just UTF-8."
    }
  }
}
