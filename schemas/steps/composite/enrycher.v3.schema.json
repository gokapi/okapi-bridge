{
  "$id": "enrycher",
  "title": "Enrycher",
  "type": "object",
  "description": "Applies Enrycher ITS annotations to the source content. Expects: filter events. Sends back: filter events.",
  "x-step": {
    "class": "net.sf.okapi.steps.enrycher.EnrycherStep",
    "parameterMappings": [
      "SOURCE_LOCALE",
      "TARGET_LOCALE"
    ],
    "eventHandlers": [
      "handleStartBatch"
    ],
    "interfaces": [],
    "inputType": "filter-events",
    "outputType": "filter-events"
  },
  "properties": {
    "baseUrl": {
      "type": "string",
      "default": "http://aidemo.ijs.si/mlw/",
      "title": "URL of the Enrycher Web service",
      "x-editor": {
        "widget": "text",
        "layout": {
          "vertical": true
        }
      },
      "description": "Endpoint URL of the Enrycher Web service to use for disambiguation annotations"
    },
    "maxEvents": {
      "type": "integer",
      "default": 20,
      "title": "Events buffer",
      "description": "Number of events to store before sending a query",
      "minimum": 1,
      "maximum": 999,
      "x-editor": {
        "widget": "spin",
        "layout": {
          "vertical": true
        }
      }
    }
  }
}
