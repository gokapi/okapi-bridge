{
  "$id": "remove-target",
  "title": "Remove Target",
  "type": "object",
  "description": "Remove targets in all or a given set of text units. Expects: filter events. Sends back: filter events.",
  "x-step": {
    "class": "net.sf.okapi.steps.common.removetarget.RemoveTargetStep",
    "parameterMappings": [
      "SOURCE_LOCALE",
      "TARGET_LOCALE"
    ],
    "eventHandlers": [
      "handleStartBatch",
      "handleTextUnit"
    ],
    "interfaces": [],
    "inputType": "filter-events",
    "outputType": "filter-events"
  },
  "properties": {
    "tusForTargetRemoval": {
      "type": "string",
      "default": "",
      "title": "Text Unit IDs for Removal",
      "x-editor": {
        "widget": "text",
        "layout": {
          "vertical": true
        },
        "text": {
          "allowEmpty": true
        }
      },
      "description": "Comma-delimited list of text unit IDs whose targets should be removed; empty removes all targets."
    },
    "targetLocalesToKeep": {
      "type": "string",
      "default": "",
      "title": "Target Locales to Keep",
      "x-editor": {
        "widget": "text",
        "layout": {
          "vertical": true
        },
        "text": {
          "allowEmpty": true
        }
      },
      "description": "Comma-delimited list of target locales to preserve; targets with other locales are removed."
    },
    "filterBasedOnIds": {
      "type": "boolean",
      "default": true,
      "title": "Filter by IDs",
      "x-editor": {
        "widget": "checkbox",
        "layout": {
          "vertical": true
        }
      },
      "description": "When true, filter text units by ID; when false, filter by target locale."
    },
    "removeTUIfNoTarget": {
      "type": "boolean",
      "default": false,
      "title": "Remove Empty Text Units",
      "x-editor": {
        "widget": "checkbox",
        "layout": {
          "vertical": true
        }
      },
      "description": "Remove the text unit entirely if it has no remaining targets after removal."
    }
  }
}
