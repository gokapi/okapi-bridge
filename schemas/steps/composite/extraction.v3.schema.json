{
  "$id": "extraction",
  "title": "Rainbow Translation Kit Creation",
  "type": "object",
  "description": "Generates a Rainbow translation kit for a batch of input documents. Expects: filter events. Sends back: filter events or raw document",
  "x-step": {
    "class": "net.sf.okapi.steps.rainbowkit.creation.ExtractionStep",
    "parameterMappings": [
      "EXECUTION_CONTEXT",
      "FILTER_CONFIGURATION_ID",
      "INPUT_ROOT_DIRECTORY",
      "INPUT_URI",
      "OUTPUT_DIRECTORY",
      "OUTPUT_ENCODING",
      "OUTPUT_URI",
      "ROOT_DIRECTORY",
      "SOURCE_LOCALE",
      "TARGET_LOCALE"
    ],
    "eventHandlers": [
      "handleEndBatch",
      "handleRawDocument",
      "handleStartBatch",
      "handleStartDocument"
    ],
    "interfaces": [],
    "inputType": "filter-events",
    "outputType": "file"
  },
  "properties": {
    "writerClass": {
      "type": "string",
      "default": "net.sf.okapi.steps.rainbowkit.xliff.XLIFFPackageWriter",
      "title": "Package Format",
      "description": "Java class of the package writer that determines the translation kit output format (e.g., XLIFF, PO, OmegaT)."
    },
    "writerOptions": {
      "type": "string",
      "default": "",
      "title": "Writer Options",
      "description": "Package writer-specific options controlling the selected output format."
    },
    "packageName": {
      "type": "string",
      "default": "pack1",
      "title": "Package Name",
      "description": "Name of the package directory to create inside the output root."
    },
    "packageDirectory": {
      "type": "string",
      "default": "${inputRootDir}",
      "title": "Output Root Directory",
      "description": "Root directory where the translation package will be created; supports ${inputRootDir} and locale variables."
    },
    "message": {
      "type": "string",
      "default": "",
      "title": "Package Message",
      "description": "Optional message or instructions to include with the translation package."
    },
    "outputManifest": {
      "type": "boolean",
      "default": true,
      "title": "Output Manifest",
      "description": "Generate a manifest file required for merging the translated kit back into the original format."
    },
    "createZip": {
      "type": "boolean",
      "default": false,
      "title": "Create ZIP Package",
      "description": "Create a ZIP archive (.rkp) of the generated translation package."
    },
    "sendOutput": {
      "type": "boolean",
      "default": false,
      "title": "Send Output to Next Step",
      "description": "Send prepared files as raw documents to the next pipeline step instead of the original filter events."
    },
    "supportFiles": {
      "type": "string",
      "default": "",
      "title": "Support Material Files",
      "description": "Additional files to include in the package as support material for translators."
    }
  }
}
