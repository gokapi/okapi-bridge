{
  "$id": "diff-leverage",
  "title": "Diff Leverage",
  "type": "object",
  "description": "Compare two source documents (i.e., different versions) and copy the old target content when we find a match. Can be a monolingual and bi-lingual input or three monolingual inputs. Paragraphs (TextUnits) must align in all cases",
  "x-step": {
    "class": "net.sf.okapi.steps.diffleverage.DiffLeverageStep",
    "parameterMappings": [
      "FILTER_CONFIGURATION_MAPPER",
      "SECOND_INPUT_RAWDOC",
      "SOURCE_LOCALE",
      "TARGET_LOCALE",
      "THIRD_INPUT_RAWDOC"
    ],
    "eventHandlers": [
      "handleDocumentPart",
      "handleEndBatch",
      "handleEndDocument",
      "handleEndGroup",
      "handleEndSubDocument",
      "handleRawDocument",
      "handleStartBatch",
      "handleStartDocument",
      "handleStartGroup",
      "handleStartSubDocument",
      "handleTextUnit"
    ],
    "interfaces": [],
    "inputType": "filter-events",
    "outputType": "filter-events"
  },
  "properties": {
    "fuzzyThreshold": {
      "type": "integer",
      "default": 100,
      "title": "Leverage only if the match is equal or above this score",
      "description": "Fuzzy Thresholds are between 1 and 100. A score of 100 emans exact match (codes and text) only",
      "minimum": 1,
      "maximum": 100,
      "x-editor": {
        "widget": "spin"
      }
    },
    "codesensitive": {
      "type": "boolean",
      "default": true,
      "title": "Include inline codes in the comparison",
      "description": "Use codes to compare contents",
      "x-editor": {
        "widget": "checkbox",
        "layout": {
          "vertical": true
        }
      }
    },
    "diffOnly": {
      "type": "boolean",
      "default": false,
      "title": "Diff only and mark the TextUnit as matched",
      "description": "Diff only and do not copy the match or create a leverage annotation",
      "x-editor": {
        "widget": "checkbox",
        "layout": {
          "vertical": true
        }
      }
    },
    "copyToTarget": {
      "type": "boolean",
      "default": false,
      "title": "Copy to/over the target? (WARNING: Copied target will not be segmented!)",
      "description": "Copy to/over the target (a leverage annotation will still be created). WARNING: Copied target will not be segmented and any exisiting target will be lost.",
      "x-editor": {
        "widget": "checkbox",
        "layout": {
          "vertical": true
        }
      }
    }
  }
}