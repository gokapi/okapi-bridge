{
  "$id": "uri-conversion",
  "title": "URI Conversion",
  "type": "object",
  "description": "Encodes or Decodes URI escape sequences. Expects: filter events. Sends back: filter events.",
  "x-step": {
    "class": "net.sf.okapi.steps.uriconversion.UriConversionStep",
    "parameterMappings": [
      "SOURCE_LOCALE",
      "TARGET_LOCALE"
    ],
    "eventHandlers": [
      "handleTextUnit"
    ],
    "interfaces": [],
    "inputType": "filter-events",
    "outputType": "filter-events"
  },
  "properties": {
    "conversionType": {
      "type": "integer",
      "default": 0,
      "title": "Conversion Direction",
      "description": "Whether to un-escape (decode) or escape (encode) URI sequences."
    },
    "updateAll": {
      "type": "boolean",
      "default": false,
      "title": "Escape All Extended Characters",
      "description": "Convert all non-ASCII characters to URI escape sequences."
    },
    "escapeList": {
      "type": "string",
      "default": "%{}[]()&",
      "title": "Characters to Escape",
      "description": "The set of characters to convert to URI escape sequences."
    }
  }
}
