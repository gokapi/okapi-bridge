{
  "$id": "exact-text-only-character-count",
  "title": "Exact Text Only Match Character Count",
  "type": "object",
  "description": "Matches text exactly, but there is a difference in one or more codes. Expects: filter events. Sends back: filter events.",
  "x-step": {
    "class": "net.sf.okapi.steps.wordcount.categorized.okapi.ExactTextOnlyCharacterCountStep",
    "parameterMappings": [
      "SOURCE_LOCALE",
      "TARGET_LOCALE"
    ],
    "eventHandlers": [],
    "interfaces": [],
    "inputType": "filter-events",
    "outputType": "filter-events"
  }
}