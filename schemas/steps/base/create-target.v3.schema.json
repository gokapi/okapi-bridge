{
  "$id": "create-target",
  "title": "Create Target",
  "type": "object",
  "description": "Create target segment container. Optionally copy source content to the target. Expects: filter events. Sends back: filter events.",
  "x-step": {
    "class": "net.sf.okapi.steps.common.createtarget.CreateTargetStep",
    "parameterMappings": [
      "SOURCE_LOCALE",
      "TARGET_LOCALE"
    ],
    "eventHandlers": [
      "handleTextUnit"
    ],
    "interfaces": [],
    "inputType": "filter-events",
    "outputType": "filter-events"
  },
  "properties": {
    "copyContent": {
      "type": "boolean",
      "default": true,
      "title": "Copy the source content to the target",
      "x-editor": {
        "widget": "checkbox",
        "layout": {
          "vertical": true
        }
      }
    },
    "copyProperties": {
      "type": "boolean",
      "default": true,
      "title": "Copy the source properties to the target",
      "x-editor": {
        "widget": "checkbox",
        "layout": {
          "vertical": true
        }
      }
    },
    "overwriteExisting": {
      "type": "boolean",
      "default": false,
      "title": "Overwrite the current target content",
      "x-editor": {
        "widget": "checkbox",
        "layout": {
          "vertical": true
        }
      }
    },
    "createOnNonTranslatable": {
      "type": "boolean",
      "default": true,
      "title": "Creates target for non-translatable text units",
      "x-editor": {
        "widget": "checkbox",
        "layout": {
          "vertical": true
        }
      }
    }
  }
}